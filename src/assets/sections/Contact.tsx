import { SectionTitle } from "../components/SectionTitle"

const Contact = () => {

  return (
    <section className={'h-module-medium py-36 px-36'}>
      <div>
        <SectionTitle sectionTitle="Let's talk." sectionVariant={true}/>
      </div>
    </section>
  )
}

export default Contact